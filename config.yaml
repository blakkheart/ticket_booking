db:
  host: localhost
  port: 5432
  user: wowsp
  name: booking
  password: wowsp
  sslmode: disable

server:
  host: 0.0.0.0
  port: 8080

auth:
  secret_key: supersecret
  token_duration: 24h

payment:
  provider: mock # stripe etc
  secret: "test_secret"